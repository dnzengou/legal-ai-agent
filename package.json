{
  "name": "legal-ai-agent-site",
  "version": "0.7.0",
  "private": true,
  "description": "Static landing page for legal-ai-agent (the product is the Python API).",
  "scripts": {
    "build": "mkdir -p dist && cp -r site/. dist/"
  }
}
